import { describe } from "yargs";
import { storeState } from "./../src/index.js";

describe('storeState', () => {

    test('should show the empty states of the plants', () => {
        const example = new storeState();
    })
})

